# Dependencies
node_modules/

# Personal plugin settings (positions, provider config) — never publish
data.json

# macOS
.DS_Store

# Build artifact — BRAT installs from release assets, not the repo tree
main.js
